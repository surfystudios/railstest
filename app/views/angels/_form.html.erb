<%= form_with(model: angel, local: true) do |form| %>
  <% if angel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(angel.errors.count, "error") %> prohibited this angel from being saved:</h2>

      <ul>
      <% angel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :angel_name %>
  </div>

  <div class="field">
    <%= form.label :wings %>
    <%= form.text_field :wings, id: :angel_wings %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
